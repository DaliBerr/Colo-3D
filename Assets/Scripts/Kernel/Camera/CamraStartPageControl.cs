using UnityEngine;

namespace Kernel
{
    public class StartPageCamera : CameraControl
    {
        // public override Camera CameraComponent { get; set; }
        // // private override int zoomSpeed => 0;  
        // void Start()
        // {
        //     CameraComponent = GetComponentInChildren<Camera>();
        //     zoomSpeed = 0f;
        //     panSpeed = 0f;

        // }

        // protected override void HandlePan()
        // {
            
        //     // float moveX = Random.Range(-0.5f, 0.5f) * panSpeed * Time.deltaTime;
        //     // float moveY = Random.Range(-0.5f, 0.5f) * panSpeed * Time.deltaTime;
        // }
    }

}